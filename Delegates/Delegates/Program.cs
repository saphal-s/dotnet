namespace Delegates
{
    public class Program
    { 
        public static void Main(string[] args)
        {
            // Func<int, int, int> Operation = (x, y) => x + y; single line
           Predicate<int> Operation = a =>//use return bollean
           {
               return true;
           };
            
            
        }       
    }
}